# Puerto HTTP donde escucha el servidor Express.
# Valores válidos: cualquier puerto TCP libre (recomendado: 3001 para desarrollo local).
PORT=3001

# Modo de ejecución usado por Node y Express.
# Valores válidos: development | test | production
NODE_ENV=development
